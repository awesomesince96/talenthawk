# TalentHawk — copy to `.env` in this directory and set values.
# `.env` is gitignored; do not commit real keys.

# SerpAPI (optional) — needed for “SerpAPI” or “Remotive + SerpAPI” in the sidebar.
# https://serpapi.com/
SERPAPI_API_KEY=

# Optional alias for the same key:
# SERPAPI_KEY=
